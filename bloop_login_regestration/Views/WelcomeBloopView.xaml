<UserControl x:Class="bloop_login_regestration.Views.WelcomeBloopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:bloop_login_regestration.Views"
             mc:Ignorable="d" 
                      d:DesignHeight="550" d:DesignWidth="875">
    <Grid>
        <Border Width="575"
    Height="250"
    Background="#4C272739"
    CornerRadius="50"
    BorderThickness="1"
    BorderBrush="#33FFFFFF"
    HorizontalAlignment="Center"
    VerticalAlignment="Center">
            <Grid>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40" >

                    <TextBlock FontSize="22"
                               FontWeight="Bold"
                               FontFamily="Inter"
                               Foreground="White"
                               Margin="0,0,0,5"
                               TextAlignment="Center">
        <Run Text="Вітаємо в"/>
        <Run Text="Bloop!" Foreground="#00FFF0"/>
                    </TextBlock>

                    <TextBlock FontSize="11"
               Foreground="LightGray"
               FontFamily="Inter"
               Margin="0,0,0,45"
               TextAlignment="Center">
                Твій акаунт створено. Тепер Bloop — теж твоє місце.
                    </TextBlock>

                    <!-- Кнопка -->
                    <Button x:Name="btnBackLogin"
                        Content="Почати спілкування"
                        FontSize="14"
                        FontFamily="Montserrat"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Cursor="Hand"
                        Width="240"
                        Height="35"
                        Margin="0,0,0,10"
                        Click="StartCommunication_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#FF006DEA"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF173856"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="20">
                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
