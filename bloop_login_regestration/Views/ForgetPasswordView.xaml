<UserControl x:Class="bloop_login_regestration.Views.ForgetPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:bloop_login_regestration.Views"
             xmlns:viewModel="clr-namespace:bloop_login_regestration.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="875"
             >
    <UserControl.DataContext>
        <viewModel:ForgetPasswordViewModel />
    </UserControl.DataContext>


    <Grid>
        <Border Width="600"
            Height="380"
            Background="#4C272739"
            CornerRadius="50"
            BorderThickness="1"
            BorderBrush="#33FFFFFF"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Grid Margin="40">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Забув(-ла) пароль?"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="White"
                           FontFamily="Montserrat"
                           Margin="0,0,0,8"
                           TextAlignment="Center"/>

                    <TextBlock FontSize="13"
                           Foreground="LightGray"
                           FontFamily="Montserrat"
                           TextAlignment="Center"
                           Margin="0,0,0,4">
                Та не страшно. У всіх вилітає з голови.
                    </TextBlock>

                    <TextBlock FontSize="13"
                           Foreground="LightGray"
                           FontFamily="Montserrat"
                           TextAlignment="Center"
                           Margin="0,0,0,35">
                Напиши свій email — і ми надішлемо інструкцію.
                    </TextBlock>

                    <!-- Email -->
                    <StackPanel Width="250">
                        <TextBlock Text="Електронна пошта" 
                           Foreground="LightGray"
                           FontSize="12"
                           FontFamily="Montserrat"
                           Margin="0,0,0,5"/>

                        <TextBox x:Name="txtEmail"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                         Height="40" Width="250"
                         Background="#10FFFFFF"
                         BorderBrush="#444A59"
                         BorderThickness="1"
                         Foreground="White"
                         Padding="10"
                         FontFamily="Montserrat"
                         HorizontalAlignment="Left"
                         Margin="0,0,0,40">
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <Border CornerRadius="20"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                        <ScrollViewer x:Name="PART_ContentHost" />
                                    </Border>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                    </StackPanel>
                    <!-- Кнопка "Надіслати" -->
                    <Button x:Name="btnSubmit"
                        Content="Надіслати"
                        Command="{Binding SendResetCommand}"
                        FontSize="14"
                        FontFamily="Montserrat"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Cursor="Hand"
                        Margin="0,0,0,10"
                        HorizontalAlignment="Center"
                        Width="250">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#FF006DEA"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF173856"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Height="44"
                                    CornerRadius="20"
                                    Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center"
                                                HorizontalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- Посилання -->
                    <TextBlock FontSize="11"
                           FontFamily="Montserrat"
                           Foreground="LightGray"
                           TextAlignment="Center">
                    <Run Text="Згадав(-ла) пароль? " />
                    <Hyperlink Click="BackLogin_Click"
                               Cursor="Hand"
                               Foreground="#00FFF0"
                               TextDecorations="{x:Null}">
                        Повернутись до входу
                        <Hyperlink.Style>
                            <Style TargetType="Hyperlink">
                                <Setter Property="TextDecorations" Value="{x:Null}" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="TextDecorations" Value="Underline"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Hyperlink.Style>
                    </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
